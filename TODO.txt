edit
	discard is bugged, wrong values are reset

CanDeactivate route guard to save pending changes
	requires dirty checks in place inside ActivityEdit Component

routing
	different "layout" based on loggedin state
		=> Angular Routing (Deborah Kurata) 4h:49m

	PageNotFoundComponent when searching an activity that does not exist

delete with confirmation dialog
	there is a bug!
	
minor
	byCreationDateRangeFilter

optional
	throttle byCategoryFilter searches
		need a string based criteria

	update dependencies